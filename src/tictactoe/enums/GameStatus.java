package src.tictactoe.enums;

public enum GameStatus {

    // By making gamestatus enum we have now
    // power to add multiple gameStatus if future requriements come
    // like PLAY_WIN_ANIMATION, HISTORY_PLAYING
    IN_PROGRESS, WIN, DRAW
}
